

<h1>{{user.first_name}} {{user.last_name}} id# {{user.id}}</h1>

<ul class='list-group'>
    <li class='list-group-item'><strong>Email: </strong>{{user.email}}</li>
    <li class='list-group-item'><strong>Phone: </strong>{{user.phone}}</li>
</ul>


<a class='btn btn-default pull-left' style="background: #aaa; margin-top: 10px;" href='/'>Back</a>
<form method="POST" action="/user/delete/{{user.id}}?_method=DELETE">
    <input type="submit" class="btn btn-danger mt-1" value="Delete">
</form>